# ── 1. storefront-bff: chỉ storefront-ui và swagger-ui được gọi vào
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-storefront-bff
  namespace: yas-staging
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: storefront-bff
  action: ALLOW
  rules:
  - from:
    - source:
        principals:
        - "cluster.local/ns/yas-staging/sa/storefront-ui"
        - "cluster.local/ns/yas-staging/sa/yas-staging-swagger-ui"
        - "cluster.local/ns/istio-system/sa/istio-ingressgateway-service-account"
---
# ── 2. backoffice-bff: chỉ backoffice-ui và swagger-ui
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-backoffice-bff
  namespace: yas-staging
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: backoffice-bff
  action: ALLOW
  rules:
  - from:
    - source:
        principals:
        - "cluster.local/ns/yas-staging/sa/backoffice-ui"
        - "cluster.local/ns/yas-staging/sa/yas-staging-swagger-ui"
        - "cluster.local/ns/istio-system/sa/istio-ingressgateway-service-account"
---
# ── 3. product: BFF hai chiều + order + cart + search + sampledata
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-product
  namespace: yas-staging
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: product
  action: ALLOW
  rules:
  - from:
    - source:
        principals:
        - "cluster.local/ns/yas-staging/sa/storefront-bff"
        - "cluster.local/ns/yas-staging/sa/backoffice-bff"
        - "cluster.local/ns/yas-staging/sa/order"
        - "cluster.local/ns/yas-staging/sa/cart"
        - "cluster.local/ns/yas-staging/sa/search"
        - "cluster.local/ns/yas-staging/sa/sampledata"
---
# ── 4. cart: storefront-bff (user) + order (checkout flow)
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-cart
  namespace: yas-staging
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: cart
  action: ALLOW
  rules:
  - from:
    - source:
        principals:
        - "cluster.local/ns/yas-staging/sa/storefront-bff"
        - "cluster.local/ns/yas-staging/sa/order"
---
# ── 5. order: chỉ hai BFF tạo đơn hàng được phép
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-order
  namespace: yas-staging
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: order
  action: ALLOW
  rules:
  - from:
    - source:
        principals:
        - "cluster.local/ns/yas-staging/sa/storefront-bff"
        - "cluster.local/ns/yas-staging/sa/backoffice-bff"
---
# ── 6. customer: BFF + order (lấy địa chỉ giao hàng) + sampledata
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-customer
  namespace: yas-staging
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: customer
  action: ALLOW
  rules:
  - from:
    - source:
        principals:
        - "cluster.local/ns/yas-staging/sa/storefront-bff"
        - "cluster.local/ns/yas-staging/sa/backoffice-bff"
        - "cluster.local/ns/yas-staging/sa/order"
        - "cluster.local/ns/yas-staging/sa/sampledata"
---
# ── 7. inventory: backoffice-bff (quản lý kho) + order + cart + sampledata
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-inventory
  namespace: yas-staging
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: inventory
  action: ALLOW
  rules:
  - from:
    - source:
        principals:
        - "cluster.local/ns/yas-staging/sa/backoffice-bff"
        - "cluster.local/ns/yas-staging/sa/order"
        - "cluster.local/ns/yas-staging/sa/cart"
        - "cluster.local/ns/yas-staging/sa/sampledata"
---
# ── 8. tax: BFF (hiển thị giá) + order (tính thuế khi checkout)
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-tax
  namespace: yas-staging
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: tax
  action: ALLOW
  rules:
  - from:
    - source:
        principals:
        - "cluster.local/ns/yas-staging/sa/storefront-bff"
        - "cluster.local/ns/yas-staging/sa/backoffice-bff"
        - "cluster.local/ns/yas-staging/sa/order"
---
# ── 9. media: BFF upload/xem ảnh + sampledata seed ảnh ban đầu
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-media
  namespace: yas-staging
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: media
  action: ALLOW
  rules:
  - from:
    - source:
        principals:
        - "cluster.local/ns/yas-staging/sa/storefront-bff"
        - "cluster.local/ns/yas-staging/sa/backoffice-bff"
        - "cluster.local/ns/yas-staging/sa/product"
        - "cluster.local/ns/yas-staging/sa/sampledata"
---
# ── 10. search: hai BFF tìm kiếm sản phẩm
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: allow-search
  namespace: yas-staging
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: search
  action: ALLOW
  rules:
  - from:
    - source:
        principals:
        - "cluster.local/ns/yas-staging/sa/storefront-bff"
        - "cluster.local/ns/yas-staging/sa/backoffice-bff"
