apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: backoffice-public-routes
  namespace: yas-staging
spec:
  hosts:
    - backoffice.staging.yas.local.com
  gateways:
    - yas-staging-public-gateway
  http:
    - match:
        - uri:
            prefix: /authentication
        - uri:
            prefix: /oauth2
        - uri:
            prefix: /login/oauth2
        - uri:
            exact: /logout
      route:
        - destination:
            host: backoffice-bff
            port:
              number: 80
    - match:
        - uri:
            prefix: /api
      route:
        - destination:
            host: backoffice-bff
            port:
              number: 80
    - route:
        - destination:
            host: backoffice-ui
            port:
              number: 3000
